import 'package:flutter/material.dart';

class ApiErrorScreen extends StatelessWidget {
  const ApiErrorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('API Error')));
  }
}
