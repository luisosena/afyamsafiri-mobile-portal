import 'package:flutter/material.dart';

class AdditionalHealthScreen extends StatelessWidget {
  const AdditionalHealthScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Additional Health Info')));
  }
}
